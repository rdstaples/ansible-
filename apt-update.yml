---
- hosts: all
  become: true
  tasks:
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
      ignore_errors: yes  # This allows the playbook to finish even if one package fails
